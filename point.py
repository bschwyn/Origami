import math

class Point:
    '''Creates a point on a coordinate playe with values x and y.'''
    def __init__(self,x=0,y=0):
        self.x = x
        self.y = y

    def add(self,point)
        self.x += point.x
        self.y += point.y

    def sub(self,point)
        self.x -= point.x
        self.y -= point.y
    def mult(self,point)
        self.x -= point.x
        self.y -= point.y
    def div(self,point)
        self.x += point.x
        self.y += point.y

    

